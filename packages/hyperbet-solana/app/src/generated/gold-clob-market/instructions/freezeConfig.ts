/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { GOLD_CLOB_MARKET_PROGRAM_ADDRESS } from '../programs/index.js';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared/index.js';

export const FREEZE_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([30, 68, 20, 154, 197, 42, 47, 122]);

export function getFreezeConfigDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(FREEZE_CONFIG_DISCRIMINATOR); }

export type FreezeConfigInstruction<TProgram extends string = typeof GOLD_CLOB_MARKET_PROGRAM_ADDRESS, TAccountAuthority extends string | AccountMeta<string> = string, TAccountConfig extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig, ...TRemainingAccounts]>;

export type FreezeConfigInstructionData = { discriminator: ReadonlyUint8Array;  };

export type FreezeConfigInstructionDataArgs = {  };

export function getFreezeConfigInstructionDataEncoder(): FixedSizeEncoder<FreezeConfigInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: FREEZE_CONFIG_DISCRIMINATOR }));
}

export function getFreezeConfigInstructionDataDecoder(): FixedSizeDecoder<FreezeConfigInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getFreezeConfigInstructionDataCodec(): FixedSizeCodec<FreezeConfigInstructionDataArgs, FreezeConfigInstructionData> {
    return combineCodec(getFreezeConfigInstructionDataEncoder(), getFreezeConfigInstructionDataDecoder());
}

export type FreezeConfigAsyncInput<TAccountAuthority extends string = string, TAccountConfig extends string = string> =  {
  authority: TransactionSigner<TAccountAuthority>;
config?: Address<TAccountConfig>;
}

export async function getFreezeConfigInstructionAsync<TAccountAuthority extends string, TAccountConfig extends string, TProgramAddress extends Address = typeof GOLD_CLOB_MARKET_PROGRAM_ADDRESS>(input: FreezeConfigAsyncInput<TAccountAuthority, TAccountConfig>, config?: { programAddress?: TProgramAddress } ): Promise<FreezeConfigInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>> {
  // Program address.
const programAddress = config?.programAddress ?? GOLD_CLOB_MARKET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { authority: { value: input.authority ?? null, isWritable: false }, config: { value: input.config ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;


// Resolve default values.
if (!accounts.config.value) {
accounts.config.value = await getProgramDerivedAddress({ programAddress, seeds: [getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103]))] });
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.authority), getAccountMeta(accounts.config)], data: getFreezeConfigInstructionDataEncoder().encode({}), programAddress } as FreezeConfigInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>);
}

export type FreezeConfigInput<TAccountAuthority extends string = string, TAccountConfig extends string = string> =  {
  authority: TransactionSigner<TAccountAuthority>;
config: Address<TAccountConfig>;
}

export function getFreezeConfigInstruction<TAccountAuthority extends string, TAccountConfig extends string, TProgramAddress extends Address = typeof GOLD_CLOB_MARKET_PROGRAM_ADDRESS>(input: FreezeConfigInput<TAccountAuthority, TAccountConfig>, config?: { programAddress?: TProgramAddress } ): FreezeConfigInstruction<TProgramAddress, TAccountAuthority, TAccountConfig> {
  // Program address.
const programAddress = config?.programAddress ?? GOLD_CLOB_MARKET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { authority: { value: input.authority ?? null, isWritable: false }, config: { value: input.config ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.authority), getAccountMeta(accounts.config)], data: getFreezeConfigInstructionDataEncoder().encode({}), programAddress } as FreezeConfigInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>);
}

export type ParsedFreezeConfigInstruction<TProgram extends string = typeof GOLD_CLOB_MARKET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
authority: TAccountMetas[0];
config: TAccountMetas[1];
};
data: FreezeConfigInstructionData; };

export function parseFreezeConfigInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedFreezeConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { authority: getNextAccount(), config: getNextAccount() }, data: getFreezeConfigInstructionDataDecoder().decode(instruction.data) };
}