/* eslint-disable */
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, type AccountMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type WritableAccount } from '@solana/kit';
import { GOLD_CLOB_MARKET_PROGRAM_ADDRESS } from '../programs/index.js';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared/index.js';

export const SYNC_MARKET_FROM_DUEL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([235, 180, 137, 53, 242, 12, 85, 213]);

export function getSyncMarketFromDuelDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(SYNC_MARKET_FROM_DUEL_DISCRIMINATOR); }

export type SyncMarketFromDuelInstruction<TProgram extends string = typeof GOLD_CLOB_MARKET_PROGRAM_ADDRESS, TAccountMarketState extends string | AccountMeta<string> = string, TAccountDuelState extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountMarketState extends string ? WritableAccount<TAccountMarketState> : TAccountMarketState, TAccountDuelState extends string ? ReadonlyAccount<TAccountDuelState> : TAccountDuelState, ...TRemainingAccounts]>;

export type SyncMarketFromDuelInstructionData = { discriminator: ReadonlyUint8Array;  };

export type SyncMarketFromDuelInstructionDataArgs = {  };

export function getSyncMarketFromDuelInstructionDataEncoder(): FixedSizeEncoder<SyncMarketFromDuelInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: SYNC_MARKET_FROM_DUEL_DISCRIMINATOR }));
}

export function getSyncMarketFromDuelInstructionDataDecoder(): FixedSizeDecoder<SyncMarketFromDuelInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getSyncMarketFromDuelInstructionDataCodec(): FixedSizeCodec<SyncMarketFromDuelInstructionDataArgs, SyncMarketFromDuelInstructionData> {
    return combineCodec(getSyncMarketFromDuelInstructionDataEncoder(), getSyncMarketFromDuelInstructionDataDecoder());
}

export type SyncMarketFromDuelInput<TAccountMarketState extends string = string, TAccountDuelState extends string = string> =  {
  marketState: Address<TAccountMarketState>;
duelState: Address<TAccountDuelState>;
}

export function getSyncMarketFromDuelInstruction<TAccountMarketState extends string, TAccountDuelState extends string, TProgramAddress extends Address = typeof GOLD_CLOB_MARKET_PROGRAM_ADDRESS>(input: SyncMarketFromDuelInput<TAccountMarketState, TAccountDuelState>, config?: { programAddress?: TProgramAddress } ): SyncMarketFromDuelInstruction<TProgramAddress, TAccountMarketState, TAccountDuelState> {
  // Program address.
const programAddress = config?.programAddress ?? GOLD_CLOB_MARKET_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { marketState: { value: input.marketState ?? null, isWritable: true }, duelState: { value: input.duelState ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta(accounts.marketState), getAccountMeta(accounts.duelState)], data: getSyncMarketFromDuelInstructionDataEncoder().encode({}), programAddress } as SyncMarketFromDuelInstruction<TProgramAddress, TAccountMarketState, TAccountDuelState>);
}

export type ParsedSyncMarketFromDuelInstruction<TProgram extends string = typeof GOLD_CLOB_MARKET_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
marketState: TAccountMetas[0];
duelState: TAccountMetas[1];
};
data: SyncMarketFromDuelInstructionData; };

export function parseSyncMarketFromDuelInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSyncMarketFromDuelInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  // TODO: Coded error.
  throw new Error('Not enough accounts');
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { marketState: getNextAccount(), duelState: getNextAccount() }, data: getSyncMarketFromDuelInstructionDataDecoder().decode(instruction.data) };
}